import angular from 'angular';

/**
 * @namespace app/store
 * @desc Manages access of global application data
 *   and controls how that data can be modified.
 */
export default angular.module('app.store', []).name;
